#include "../lat_stdlib.h"

// An example of recursion. A sort of reverse function.
letrec ( f , | x . if x then ( nil . f ( < x ) ) else x end ,

    f ( ( ( ( ( nil . nil ) . nil ) . nil ) . nil ) . nil )
)
